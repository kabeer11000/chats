import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kabeersnetwork.chats.lite',
  appName: 'Chats',
  webDir: 'web-stable/build',
  bundledWebRuntime: true
};

export default config;
